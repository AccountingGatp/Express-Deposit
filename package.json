{
  "name": "express-text-sales",
  "version": "1.0.0",
  "description": "Web app that turns an Authorize.net monthly sales export into a QuickBooks-ready sales-entry workbook for Express Text.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:offline": "vite build && node build-offline.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
